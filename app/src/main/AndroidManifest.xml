<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uiactivity2nd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UIActivity2nd">
        <activity android:name=".EditSong"></activity>
        <activity android:name=".EditAlbum" />
        <activity android:name=".AddSong" />
        <activity android:name=".AddAlbum" />
        <activity android:name=".Albums" />
        <activity android:name=".SongQueue" />
        <activity android:name=".SongInfo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>